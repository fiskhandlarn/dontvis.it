{
  "minimum-stability": "dev",
  "prefer-stable": true,
  "name": "fiskhandlarn/unvis.it",
  "description": "Unvis.it, the idiot circumventor tool",
  "license": "MIT",
  "require": {
    "php": "^7.2",
    "vlucas/phpdotenv": "^4.1"
  },
  "autoload": {
    "files": [
      "src/helpers.php"
    ]
  },
  "scripts": {
    "post-install-cmd": [
      "php -r \"file_exists('.env') || copy('.env.example', '.env');\""
    ]
  }
}
